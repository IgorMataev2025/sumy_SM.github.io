-- Портал послуг Safina — міграція 010: статистика для постачальника (п.17, наступна фіча
-- понад MVP після карти каталогу, постановка робочої тестової версії, 2026-09-12).
-- ViewCount — лічильник переглядів ServiceDetails.aspx, +1 при кожному не-постбек
-- завантаженні опублікованого оголошення (включно з переглядами самого власника —
-- свідомо не фільтруємо, простіше і чесніше по факту трафіку). Розмови (Messages),
-- вподобання (Favorites) і відгуки (Reviews) для статистики рахуються "на льоту" з
-- наявних таблиць — окремих лічильників для них не потрібно.
-- Застосування: mysql --default-character-set=utf8mb4 -u root -p sumy_portal < migration_010_service_stats.sql

ALTER TABLE Services
    ADD COLUMN ViewCount INT NOT NULL DEFAULT 0;
